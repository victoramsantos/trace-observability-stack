type: "OTLP"
config:
  client_type: "grpc"
  compression: "gzip"
  insecure: true
  endpoint: "otel-collector:4317"
  timeout: 1s
  retry_config:
    retry_enabled: false
  tls_config:
    insecure_skip_verify: false